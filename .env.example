# MongoDB connection string used by Mongoose (index.js: process.env.DB_HOST)
DB_HOST=mongodb://localhost:27017/rufuf

# Secret used to sign/verify JWT access tokens (controllers/auth.js)
jwtPrivateKey=change-me-to-a-long-random-secret

# HTTP port (optional, defaults to 3000)
PORT=3000
